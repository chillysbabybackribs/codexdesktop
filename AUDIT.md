
- 2026-07-19: image_url 400 fix (codex-config.ts) is prompt-guidance-only — adds instruction to regex-extract `data:image/...` from screenshot tool result before calling `image()`. Mitigation, not deterministic: relies on model obeying guidance every time; root cause (tool result concatenates JSON metadata + data URI) is not fixed at code level. Consider sanitizing in the image() handler / tool result if reachable. Regex `data:image/[^\s]+` works only because the URI is last in the string.

- 2026-07-19: Agent-per-tab scoping orphans agents on tab close. `handleCloseMainChatTab` (App.tsx:~1305) never touches agent sessions; close guard checks the MAIN tab's `status==='working'`, not owned agents'. Result: an agent mid-run whose tab closes stays running but is unreachable in every tab (render filter is strict `mainChatTabKey === activeTabKey`), cannot be stopped/promoted; on restart `restoreAgentDock`'s `mainChatTabKeys.has(...)` filter silently discards the persisted record. Fix: on tab close, stop+close owned agents or reassign them to the surviving active tab (same policy as legacy records).

- 2026-07-19: Provider-agnostic claim is only half true. Renderer keys off item types, but the Claude adapter (claude-events.ts:178–199) flattens EVERY tool_use → generic `mcpToolCall` (server:'claude'), never emitting `commandExecution` or `fileChange`. Consequence: Claude turns lose three Cursor-defining tool surfaces, not just diff cards — (1) highlighted diff cards, (2) `CompactActionRows` read/list/search one-liners (TaskActivity.tsx:508, requires `CommandExecutionItem.commandActions`), (3) `+adds −dels` turn-diff summary (fileChange-derived, TaskActivity.tsx:1365). Prose/reasoning/markdown/fences ARE fully shared. Real fix = adapter maps Claude Edit/Write→fileChange and Bash→commandExecution with parsed actions, not more renderer polish.

- 2026-07-19: Cursor-parity synthesis ranks UI chrome (thinking-text collapse, cards) as the "feels like Cursor" gap, but Cursor's actual moat is semantic codebase indexing + apply-model quality — the retrieval that makes edits land, not chat chrome. Risk: shipping thinking-text polish and still not feeling like Cursor because context-gathering is the differentiator. Secondary: recommendations lean on Cursor docs/changelog that churn fast; snapshot-date the sources or they go stale in weeks.

- 2026-07-19: `liveMainTurn` glance feature is half-wired and **breaks typecheck** (3 errors). AgentColumn requires `liveMainTurn: LiveTurnGlance | null` but App.tsx:3487 never passes it (TS2741). AgentDock.tsx also references undefined `AuditExchangeCard` (l.329) and `AgentModeSelector` (l.540). Helper `liveTurnGlance()` is exported but never called (dead). The turn's earlier `typecheck` exit 0 predates these renderer edits. Fix: pass liveMainTurn from App (compute via liveTurnGlance on the active main turn) + restore/import the two missing components, or revert.

- 2026-07-19: Tab-scoped agent windows: closing a main chat tab orphans its agents. `handleCloseMainChatTab` (App.tsx:1305) neither closes nor reassigns sessions whose `mainChatTabKey` is the closed tab, and every surface (dock render, audit trigger, buttons) filters by owner key — so a `working` agent keeps running invisibly with no way to see or stop it. On next launch `restoreAgentDock` silently drops those persisted records (`mainChatTabKeys.has()` filter), losing the thread reference. Fix: on tab close, close owned agent sessions or reassign to the surviving active tab.

- 2026-07-19: Composer mention path containment is lexical only. `MentionIndexService.read()` checks `resolve(root, path)` but then `readFile()` follows symlinks, so a tracked workspace symlink can point outside the workspace and expose arbitrary readable file contents through an `@file` mention. Resolve/validate the real path under the real workspace root (or reject symlinks) before reading; also bind IPC requests to an approved workspace rather than trusting the renderer-supplied root.

- 2026-07-19: Market-verdict audit: the "Ahead in workspace reversibility" claim is the most contestable line — mainstream competitors (Cursor checkpoints, Claude Code rewind /undo) shipped turn-level restore in 2025–26, so "ahead" needs a snapshot-dated citation of what leaders actually lack (e.g. shell-write coverage via autosnapshot watcher), not just an internal feature list. Also "582/582 tests passed" should carry the known caveat that test dirs absent from the package.json test glob silently never run — the count proves the glob is green, not that all test files execute.

- 2026-07-19 RESOLVED (market-verdict audit): reversibility claim reworded to "on pace, with a checkpoint-completeness edge over Claude Code (shell writes captured by per-turn git-index checkpoint, turn-checkpoint.ts — NOT the autosnapshot watcher); vs Cursor unproven", backed by retrieved-dated citations. Test-glob caveat independently re-verified: all 86 *.test.ts files fall under the package.json glob today; gotcha remains only for future new test dirs.

- 2026-07-19 RESOLVED (mention path containment): MentionIndexService now (1) resolves REAL paths of both the workspace root and the mention target and requires containment — a tracked symlink to an outside file resolves outside the real root and returns null, never read (regression test: src/main/mention-index.test.ts "a tracked symlink pointing outside the workspace is never read"); (2) limits file reads to paths the git index actually offered the menu, so ignored/arbitrary paths (.env etc.) are unreadable even lexically-inside; (3) validates the renderer-supplied workspace: realpath'd, must be a directory inside a git work tree or explicitly approved via the native picker dialog (workspacePick now registers approval). Folder listings were already index-derived (no disk reads). Note on residual trust: the renderer legitimately controls session cwd for full-access shells, so workspace choice itself is renderer-trusted by design; the enforced boundary is that mention reads can never leave the chosen workspace's real root nor exceed the index's surface. 5 new tests, 426/426 suite green.
- 2026-07-19: market-comparison-2026-07-19.md OpenAI Codex column is wrong-by-omission. developers.openai.com/codex 308s to learn.chatgpt.com/docs (a nav hub); research stopped at the hub and marked cells "?", but one hop deeper (learn.chatgpt.com/codex/app) documents subagents/orchestration, parallel projects, code review, browser + computer use, git worktrees, scheduled tasks — Codex app is our closest documented competitor and the browser-bucket "ahead" rating needs re-checking. Also: Cowork help URL is a hard 404 masked by Intercom's SPA shell (report said "no usable passages"); Roo shutdown claim verified correct. Runner fixes: follow one-hop nav links after cross-host redirect to a hub, and surface HTTP status in research artifacts.
