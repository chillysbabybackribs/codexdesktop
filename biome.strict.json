{
  "$schema": "https://biomejs.dev/schemas/2.5.4/schema.json",
  "//": "FINAL-PHASE ruleset. NOT active in normal `npm run lint` (that uses the quiet biome.json). Run `npm run lint:strict` during security/a11y/correctness hardening to surface the full ruleset. Extends the quiet config for files/formatter, then turns the real rules back on.",
  "extends": ["./biome.json"],
  "linter": {
    "enabled": true,
    "domains": {
      "react": "recommended"
    },
    "rules": {
      "recommended": true,
      "suspicious": {
        "noExplicitAny": "error"
      },
      "style": {
        "noNonNullAssertion": "off",
        "useImportType": "warn",
        "useNodejsImportProtocol": "warn"
      },
      "correctness": {
        "useExhaustiveDependencies": "warn",
        "noUnusedImports": "warn",
        "noUnusedVariables": "warn"
      }
    }
  }
}
